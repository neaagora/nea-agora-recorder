{
  "name": "nea-agora-recorder-extension",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npx tsc -p tsconfig.json && node -e \"const fs=require('fs'); fs.copyFileSync('src/manifest.json','dist/manifest.json'); fs.cpSync('icons','dist/icons',{recursive:true,force:true});\""
  },
  "devDependencies": {
    "@types/chrome": "^0.1.33"
  }
}
