{
  "name": "nea-agora-recorder",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "summary": "node tools/print_summary.js",
    "list": "node tools/print_summary.js --list"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.33",
    "typescript": "^5.9.3"
  }
}
